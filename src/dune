(env
 (dev
  (flags
   (:standard -warn-error -A))))

(executable
 (name main)
 (libraries fmt str yaml yaml.unix fpath containers sedlex menhirLib)
 (preprocess
  (pps sedlex.ppx)))

(menhir
 (modules parser)
 (flags --explain))
